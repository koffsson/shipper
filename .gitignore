# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE.
# Also useful when running (mainly kubectl?) commands through nohup.
*.out

# Emacs backup files
*.*~
*~

# Auto-generated files from protobuf is currently not in git repo? No,
# they are needed in order to use dep init (?)
# *.pb.go

# Binary files generated when testing should be added here!
*/consignment-service
*/consignment-cli
*/datastore
*/user-service
*/vessel-service